-- Create a new table
CREATE TABLE test (
    col1 INTEGER,
    col2 TEXT
);

-- Insert some values into the table
INSERT INTO test VALUES
    (1, 'Hello'),
    (2, 'Bye');

-- Run a very complicated query
SELECT * FROM test;
